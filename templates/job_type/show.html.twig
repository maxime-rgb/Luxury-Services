{% extends 'base.html.twig' %}

{% block title %}JobType{% endblock %}

{% block body %}


    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ job_type.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ job_type.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('job_type_index') }}"class="btn gradient grey lighten-2 black waves-effect waves-light btn-register">back to list</a>

    <a href="{{ path('job_type_edit', {'id': job_type.id}) }}">edit</a>

    {{ include('job_type/_delete_form.html.twig') }}
{% endblock %}
