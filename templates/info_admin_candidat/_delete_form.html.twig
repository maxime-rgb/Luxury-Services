<form method="post" action="{{ path('info_admin_candidat_delete', {'id': info_admin_candidat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ info_admin_candidat.id) }}">
    <button class="btn">Delete</button>
</form>
